server:
  port: 10086
spring:
  application:
    name: cloud-registry
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka
  server:
    enable-self-preservation: false     # �ر����ұ���
    eviction-interval-timer-in-ms: 5000 # ÿ��5�����һ�η����б�����
